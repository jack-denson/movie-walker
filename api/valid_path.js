// TODO: routes that consume an entire path, that validate whether the path is connected or not

// Semi-optional if this ends up just being a front-end game, not so much if we add scores & accounts and stuff